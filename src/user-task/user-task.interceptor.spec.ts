import { UserTaskInterceptor } from './user-task.interceptor';

describe('UserTaskInterceptor', () => {
  it('should be defined', () => {
    expect(new UserTaskInterceptor()).toBeDefined();
  });
});
